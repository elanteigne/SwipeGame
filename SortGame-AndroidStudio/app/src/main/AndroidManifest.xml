<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.gameproject" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StartNewGameActivity"
            android:label="@string/title_activity_start_new_game"
            android:parentActivityName=".MyActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.owner.gameproject.MyActivity" />
        </activity>
        <activity
            android:name=".OpenStoreActivity"
            android:label="@string/title_activity_open_store"
            android:parentActivityName=".MyActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.owner.gameproject.MyActivity" />
        </activity>
        <activity
            android:name=".OpenSettingsActivity"
            android:label="@string/title_activity_open_settings"
            android:parentActivityName=".MyActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.owner.gameproject.MyActivity" />
        </activity>
    </application>

</manifest>
